<div class="container">
    <div class="row">
      <div class="col s6 offset-m3">
        <h4> <%= @legopins.name %></h4>
      </div>
    </div>

    <%= render :partial => 'form' %>

    <%= link_to 'Cancel', legopin_path(@legopins) %>

</div>
